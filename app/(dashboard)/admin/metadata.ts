import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Admin Dashboard",
  description: "Manage users and monitor platform metrics (Admin only)",
  robots: {
    index: false,
    follow: false,
  },
}

